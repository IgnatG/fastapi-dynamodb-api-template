# Environment Configuration Example
# Copy this file to .env and update with your actual values

# =============================================================================
# LOCAL DEVELOPMENT SETTINGS (Required for local development)
# =============================================================================

# Basic Application Settings
PETS_HOST=127.0.0.1
PETS_PORT=8000
PETS_ENVIRONMENT=dev
PETS_LOG_LEVEL=DEBUG

# CORS Configuration (for frontend development)
# PETS_BACKEND_CORS_ORIGINS='["http://localhost:3000", "https://app.example.com"]'

# DynamoDB Local Configuration
PETS_DYNAMODB_ENDPOINT_URL=http://localhost:8001
PETS_USE_SECRETS_MANAGER=False

# =============================================================================
# AWS DEPLOYMENT SETTINGS (Only needed for AWS deployment)
# =============================================================================

# AWS Secrets Manager (Production)
# PETS_USE_SECRETS_MANAGER=true
# PETS_DYNAMODB_SECRET_NAME=pets/dynamodb
# PETS_ENVIRONMENT=lambda
# PETS_AWS_REGION=your-aws-region
