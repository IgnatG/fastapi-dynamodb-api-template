# Environment Configuration Example
# Copy this file to .env and update with your actual values

# =============================================================================
# LOCAL DEVELOPMENT SETTINGS (Required for local development)
# =============================================================================

# Basic Application Settings
APP_HOST=127.0.0.1
APP_PORT=8000
APP_ENVIRONMENT=dev
APP_LOG_LEVEL=DEBUG

# CORS Configuration (for frontend development)
# APP_BACKEND_CORS_ORIGINS='["http://localhost:3000", "https://app.example.com"]'

# DynamoDB Local Configuration
APP_DYNAMODB_ENDPOINT_URL=http://localhost:8001
APP_USE_SECRETS_MANAGER=False

# =============================================================================
# AWS DEPLOYMENT SETTINGS (Only needed for AWS deployment)
# =============================================================================

# AWS Secrets Manager (Production)
# APP_USE_SECRETS_MANAGER=true
# APP_DYNAMODB_SECRET_NAME=app/dynamodb
# APP_ENVIRONMENT=lambda
# APP_AWS_REGION=your-aws-region
