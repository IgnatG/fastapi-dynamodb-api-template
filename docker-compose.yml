services:
  api:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: app:latest
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - .:/app/src/
    depends_on:
      - db
    environment:
      APP_HOST: 0.0.0.0
      APP_DYNAMODB_ENDPOINT_URL: http://app-dynamodb:8000
  db:
    image: amazon/dynamodb-local:latest
    hostname: app-dynamodb
    restart: always
    ports:
      - "8001:8000"  # DynamoDB Local runs on port 8000 internally
    command: ["-jar", "DynamoDBLocal.jar", "-sharedDb", "-inMemory"]
    working_dir: /home/dynamodblocal



